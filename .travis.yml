# Specify the language and version
language: java
jdk:
  - openjdk8

# Remove old Ansible or other irrelevant steps
# You can remove any old sections that are no longer needed, like the following:
# before_install:
#   - <any old steps>

# Specify the build script
script:
  # Step to compile the project and build jar files
  - mvn clean package

  # Placeholder command since tests are not ready
  - echo "Tests not ready yet"
