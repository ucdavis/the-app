language: java
jdk: oraclejdk9

env:
- COVERAGE=0.35
- COVERAGE=0.5

install:
- ./gradlew clean jar

jobs:
  include:
    - stage: check
	  script: COVERAGE=0.5 ./gradlew -p ./monolithic/ui

after_success:
- find . -name jacocoTestReport.csv|xargs cat|awk -F',' '{print $3" "$4" "$5}'

deploy:
  provider: script
  script: echo 'WIP'
  on:
    all_branches: true