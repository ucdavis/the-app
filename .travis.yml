language: java
jdk: oraclejdk8
dist: trusty

before_install:
  - java -version

install:
  - ./gradlew clean jar

stages:
  - tests

jobs:
  include:
    - stage: tests
      script: ./gradlew test

notifications:
  email: false
