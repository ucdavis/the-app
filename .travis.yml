language: java
jdk: openjdk8
dist: trusty

sudo: require
services:
  - docker

before_install:
  - docker pull seo80/seo80-docker-repo:latest

install: skip
script:
  - docker run --rm microservices-finaltest ./gradlew clean jar
  - docker run --rm microservices-finaltest ./gradlew test
